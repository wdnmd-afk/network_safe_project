# 暴力破解漏洞版

漏洞版模拟一个没有失败次数阈值的教学口令检查器。

## 行为

- 只检查虚拟教学账号 `training-user`。
- 接收最多 5 个候选口令。
- 对候选口令逐个检查。
- 即使前 3 次失败，也会继续检查第 4 个候选口令。
- 攻击样例命中后返回 `brute-force-password-guessed`。

## 观察重点

连续失败次数达到 3 后，漏洞版仍然继续检查后续候选口令。这代表系统缺少登录失败保护，攻击者可以持续试探弱口令。

## 安全边界

本目录只描述本项目本机受控实验，不用于真实账号、外部系统或第三方登录入口。
