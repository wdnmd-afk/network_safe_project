# API 功能级授权 · 固定 mock 数据

本目录说明实验使用的固定虚构数据，仅用于本机受控教学观察。

- 固定用户枚举：`standard-user`（普通用户）、`admin-user`（管理员），均为虚构标识，不对应真实账户。
- 固定管理操作枚举：查看全量订单、修改他人角色等，只以稳定 key 表示，不触达真实业务。
- 固定决策 optionKey 与学习信号来自专用服务定义，页面与 API 只接受这些登记值。

mock 数据不含真实凭据、token、Cookie、角色映射或可越权访问的真实端点。
