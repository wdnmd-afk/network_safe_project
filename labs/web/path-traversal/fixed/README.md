# 修复版说明

修复版在读取虚拟文档前执行以下校验：

- 统一把反斜杠转换为 `/`。
- 处理空路径段、`.` 和 `..`。
- 得到规范化路径后，确认最终路径仍位于 `public/` 下。
- 如果路径逃逸公开根目录，直接返回 403。

当同一个受控攻击样例提交到修复版时，接口返回 `path-traversal-normalized-blocked`。

真实生产中还需要继续补充：

- 文档 ID 白名单，而不是直接暴露路径。
- 用户权限校验。
- 对象存储或下载服务隔离。
- 错误响应收敛，避免泄露目录结构。
- 访问审计和异常频率告警。
