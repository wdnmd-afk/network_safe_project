# XSS 修复版

## 目标

演示同样的客服留言输入在文本渲染下不会改变页面结构。

## 实现方式

修复版页面位于：

```text
/labs/web/xss/fixed
```

页面接收与漏洞版相同的输入，但使用文本渲染展示内容。HTML 标记会被原样显示，不会被浏览器解释为页面结构。

## 修复要点

- 用户输入默认按文本处理。
- 避免在不可信内容上使用 HTML 注入渲染。
- 后续如确需富文本，应先引入明确的白名单清洗策略。
