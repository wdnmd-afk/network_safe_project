# SSTI 修复版

修复版用于观察模板来源隔离和变量允许列表的防御效果。

## 行为

- 模板来源由系统维护。
- 用户只提供变量值。
- 只允许 `customerName`、`orderNo`、`noticeTitle` 三个变量。
- 发现受控表达式或未知表达式时返回 `ssti-expression-blocked`。

## 边界

修复版不保存完整模板、完整表达式或完整变量值，事件日志只保存脱敏学习摘要。
