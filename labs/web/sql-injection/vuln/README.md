# SQL 注入漏洞版

漏洞版模拟商品搜索接口把用户输入直接拼接进 SQL：

```sql
name LIKE '%<keyword>%'
```

当关键词为受控样例：

```text
%' OR 1=1 #
```

后续的 `AND is_hidden = 0` 条件会被注释截断，导致隐藏商品被返回。

本实验只保留读数据差异，不开放破坏性 SQL、联合查询、延时注入、文件读写等能力。
