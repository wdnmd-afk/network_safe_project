# 漏洞版说明

漏洞版模拟一个上传入口只接收文件名、MIME 和内容文本，但没有做可信校验。只要请求字段齐全，后端就会返回一个模拟存储路径。

关键观察点：

- `.php` 这类非图片扩展名没有被拒绝。
- `application/x-php` 这类非图片 MIME 没有被拒绝。
- 内容中出现 `<?php` 特征时仍然被接受。
- 统一事件日志会记录 `file-upload-executable-stored`。

本实现不会把内容写入真实文件系统，只用于观察缺少校验时的风险链路。
