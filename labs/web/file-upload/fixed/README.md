# 修复版说明

修复版在进入存储前执行三类校验：

- 扩展名必须属于 `.png`、`.jpg`、`.jpeg`、`.webp`。
- MIME 必须属于 `image/png`、`image/jpeg`、`image/webp`。
- 内容不能命中常见可执行脚本特征。

当同一个受控攻击样例提交到修复版时，接口返回 403，并记录 `file-upload-validation-blocked`。

当前修复版仍是教学模型，后续真实上传功能还应继续补充随机文件名、独立对象存储、大小限制、图片解码重写、下载域名隔离和人工审核策略。
