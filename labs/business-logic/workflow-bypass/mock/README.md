# 固定订单快照

本目录说明业务流程跳步实验使用的固定虚构订单语义：

- 订单：`SM-20260608-1099`。
- 商品：`Priority Support Plan`。
- 金额：`89`。
- 归属：`测试账户`。
- 当前风险案例：`pending` 请求直接进入 `shipping`。
- 合法阶段顺序：`pending -> paid -> shipping`。

这些信息只作为静态学习证据，不对应数据库记录，不触发真实支付、库存或物流行为。
