# LT-040 长期目标第三轮阶段审计执行文档

## 1. 目标

在 `LT-031`～`LT-039` 完成后执行第三轮事实审计，统一实验、分类、变体、深度、E2E、Web/API 入口、认证生命周期和学习体验状态，并评估 Kubernetes RBAC/IaC 与全新 Windows 发布复验的后续优先级。

## 2. 审计范围与步骤

1. 以元数据、guided 目录、coverage、entrypoint 报告和测试输出统计当前基线。
2. 逐项核对 `LT-031`～`LT-039` 的执行文档、实现与验证证据。
3. 复核搜索/路径、token 吊销、新实验安全边界和 14 分类 E2E。
4. 运行根级 `pnpm verify`、完整 E2E、专项验证和 `git diff --check`。
5. 评估下一轮优先级：先执行全新 Windows 发布复验，再实现 Kubernetes RBAC/IaC 固定审计，或反向排序；记录依据但不在本任务擅自扩展实现。
6. 更新长期目标、TODO、README、AGENTS 和覆盖矩阵，建立 `LT-041` 起后续队列。

## 3. 风险与优化

- 历史执行文档保留当时数字，只修改当前权威基线。
- 不用局部测试代替全链路证据，不把规划评估写成已实现。
- 生产 build/nginx 仅在审计判断需要且用户目标明确包含时执行；本任务要求的是优先级评估。

## 4. 完成条件

- `LT-031`～`LT-040` 均有可核查证据，当前文档无状态漂移。
- 下一轮任务、Kubernetes/IaC 和 Windows 发布复验优先级结论明确。

