{
  "name": "@network-safe/testing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/**/*.test.mjs",
    "smoke": "node src/smoke/run-smoke.mjs",
    "e2e": "playwright test -c playwright.config.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
