# Prompt 注入脚本目录

当前目录仅作为 `ai/prompt-injection` in-progress 阶段的脚本边界说明。

当前已有后端确定性路由 API 和服务端 API 测试，但本目录仍不提供脚本入口。

当前不提供：

- `exploit.py`
- `verify.ts`
- 外部 AI 调用脚本
- 模型测试脚本
- 任意提示词执行器
- 可复用绕过模板

后续如需新增只读验证脚本，必须单独编写执行文档，并且只能读取仓库内元数据、文档和受控实现文件，不得调用外部 AI、真实工具、第三方 API 或模型服务。
