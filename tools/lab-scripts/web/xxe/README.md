# XXE 脚本目录

本目录只服务 `web/xxe` 本机受控学习实验。

## 文件

- `exploit.py`：调用本机 XXE 导入预览 API 的固定样例脚本。
- `verify.ts`：输出漏洞版 / 修复版差异验证计划。

## 安全边界

- 默认只访问 `http://127.0.0.1:6667`。
- 只允许 `localhost`、`127.0.0.1`、`::1`。
- 不读取本机真实文件。
- 不请求外部目标。
- 不生成通用 XXE payload 库。
- 不解析真实外部实体。
